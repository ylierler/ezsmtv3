% ezcsp --bprolog cumulative2.ez

cspdomain(fd).

dom(0..4).

cspvar(v(D),0,10) :- dom(D).

len(D,1) :- dom(D).
res(D,1) :- dom(D).

cspvar(w,0,10).
required(w == 1).

required(cumulative([v/1],[len/2],[res/2],w)).
