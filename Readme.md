# EzsmtPlus

## Quick Start

Setup build pipeline using cmake:

```sh
mkdir build
cd build
cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON ..
```

Build the project:

``` sh
# In <project_root>/build
cmake --build .
```


